# .gitignore
# git ignore is read from top to bottom
# start by excluding the large folders we do not track
# then include (!) the tree structures and files we want to keep
# finally, ignore generic and secret files

# Specific keeps
!.gitkeep

# Generic ignores
*.log*
*.db*
*.pyc
._*
__pycache__
.env

# IDE
.vscode
.Trash*

# test directory
test

# data directories
*/data
/data

# Config defaults: ignore everything, reopen explicitly tracked files
/config/**
!config/
!config/**/
!config/**/*.gitignore
!config/**/*.gitkeep

# Authelia
!config/authelia/configuration.yml
!config/authelia/users_database.yml.template
config/authelia/authelia.log
config/authelia/db.sqlite3

# Duplicati state
config/duplicati/

# Fail2ban runtime
config/fail2ban/log/
config/fail2ban/fail2ban/fail2ban.sqlite3

# Portainer state
config/portainer/portainer.db
config/portainer/portainer.key
config/portainer/portainer.pub
config/portainer/backups/

# ddclient
!config/ddclient/ddclient.conf

# Homepage dashboard
!config/homepage/*.yaml

# SWAG nginx
!config/swag/nginx/site-confs/default.conf
!config/swag/nginx/snippets/authelia/authrequest.conf
!config/swag/nginx/snippets/authelia/location.conf
!config/swag/nginx/proxy-confs/_all.subdomain.conf
config/swag/nginx/.htpasswd
config/swag/nginx/*.pem
config/swag/nginx/*.key
config/swag/nginx/*.crt
config/swag/nginx/dhparams.pem

# Home Assistant & integrations
!config/homeassistant/automations.yaml
!config/homeassistant/scripts.yaml
!config/homeassistant/configuration.yaml
!config/homeassistant/sensors.yaml
!config/homeassistant/themes/*
!config/mosquitto/mosquitto.conf
!config/zigbee2mqtt/configuration.yaml
